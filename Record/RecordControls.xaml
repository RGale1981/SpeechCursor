<UserControl x:Class="Windows.Record.RecordControls"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:Windows.Record"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:b="clr-namespace:Windows.Behaviors"
             d:DataContext="{d:DesignInstance Type=local:RecordViewModel}"
             mc:Ignorable="d" 
             Background="Transparent">
    
    <Grid x:Name="GridHost"
          Background="Transparent" 
          Margin="4" >

        <i:Interaction.Behaviors>
            <b:DragMoveBehavior />
        </i:Interaction.Behaviors>

        <ToggleButton x:Name="ToggleRecord"
                      Content="REC"
                      Height="70"
                      Style="{StaticResource RecordToggleButtonStyle}" 
                      Width="70"
                      IsChecked="{Binding Toggled, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

    </Grid>
    
</UserControl>